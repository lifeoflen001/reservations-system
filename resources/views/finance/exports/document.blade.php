<!doctype html><html lang="en"><head><meta charset="utf-8"><title>{{ $title }} · {{ config('hotel.brand.name') }}</title><style>body{font-family:Arial,sans-serif;color:#172033;font-size:10px;margin:28px}header{display:flex;justify-content:space-between;gap:20px;border-bottom:2px solid #ef7d22;padding-bottom:12px;margin-bottom:12px}.brand{display:flex;align-items:center;gap:10px}.brand img{width:34px;height:34px;object-fit:contain}h1{font-size:20px;margin:0 0 4px;text-transform:capitalize}p{color:#68738a;margin:3px 0}header strong{font-size:15px}table{border-collapse:collapse;width:100%}th,td{border:1px solid #d8dee8;padding:6px;text-align:left;vertical-align:top}th{background:#eef3f8;font-weight:700}tr:nth-child(even){background:#fafbfd}.empty{padding:24px 0;color:#68738a}@media print{body{margin:12px}h1{font-size:16px}}</style></head><body><header><div class="brand">@if($logoData)<img src="{{ $logoData }}" alt="">@endif<div><strong>{{ config('hotel.brand.name') }}</strong><p>{{ config('hotel.defaults.property_name') }}</p><h1>{{ $title }}</h1></div></div><div><p>Generated {{ now()->format('Y-m-d H:i') }}</p><p>By {{ $generatedBy ?? 'System' }}</p>@if($from || $to)<p>Period: {{ $from ?: 'Beginning' }} to {{ $to ?: 'Today' }}</p>@endif</div></header>@if(count($rows))<table><thead><tr>@foreach($headers as $heading)<th>{{ $heading }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($row as $value)<td>{{ $value ?? '—' }}</td>@endforeach</tr>@endforeach</tbody></table>@else<p class="empty">No finance records match the selected filters.</p>@endif</body></html>
