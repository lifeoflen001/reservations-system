<!doctype html><html lang="en"><head><meta charset="utf-8"><title>{{ $title }} · {{ config('hotel.brand.name') }}</title><style>body{font-family:Arial,sans-serif;color:#172033;font-size:11px;margin:28px}h1{font-size:20px;margin:0 0 4px}p{color:#68738a;margin:0 0 18px}table{border-collapse:collapse;width:100%}th,td{border:1px solid #d8dee8;padding:7px;text-align:left;vertical-align:top}th{background:#eef3f8;font-weight:700}tr:nth-child(even){background:#fafbfd}@media print{body{margin:12px}h1{font-size:16px}}</style></head><body><h1>{{ $title }}</h1><p>{{ config('hotel.brand.name') }} · Generated {{ now()->format('Y-m-d H:i') }}</p>@if(count($rows))<table><thead><tr>@foreach(array_keys($rows[0]) as $heading)<th>{{ str_replace('_',' ',ucfirst($heading)) }}</th>@endforeach</tr></thead><tbody>@foreach($rows as $row)<tr>@foreach($row as $value)<td>{{ $value ?? '—' }}</td>@endforeach</tr>@endforeach</tbody></table>@else<p>No records match the selected report.</p>@endif</body></html>
