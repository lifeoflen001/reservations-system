@extends('layouts.guest')

@section('content')
<main class="login-page two-factor-page">
    <section class="login-panel" aria-labelledby="two-factor-title"><div class="login-content"><x-app-logo class="auth-form-logo" /><div class="login-heading"><h1 id="two-factor-title">Verify your sign-in</h1><p>Enter the six-digit code from your authenticator app to continue to HotelDesk.</p></div>@if ($errors->any())<x-feedback.alert type="danger">{{ $errors->first() }}</x-feedback.alert>@endif<form method="POST" action="{{ route('two-factor.login.store') }}" class="login-form">@csrf<x-form.input name="code" label="Authenticator code" inputmode="numeric" autocomplete="one-time-code" placeholder="000000" maxlength="6" :show-error="false" required /><x-ui.button type="submit" class="login-submit"><x-ui.icon name="shield" size="17" /> Verify and continue</x-ui.button></form><div class="two-factor-challenge-help"><strong>Lost access to your app?</strong><p>Use one of your unused recovery codes instead.</p><form method="POST" action="{{ route('two-factor.login.store') }}">@csrf<x-form.input name="recovery_code" label="Recovery code" autocomplete="off" :show-error="false" /><button class="ui-button ui-button--secondary" type="submit">Use recovery code</button></form></div><a class="auth-secondary-link" href="{{ route('login') }}">Return to sign in</a></div></section>
    <aside class="login-promo" aria-label="Account security overview"><div class="promo-content"><span class="promo-kicker">{{ config('hotel.brand.name') }} Account security</span><h2>A safer workspace for every hotel operation.</h2><p>Two-factor authentication helps keep reservations, guest records and operational data protected.</p><div class="promo-features"><article class="promo-feature"><x-ui.icon name="shield" size="22" /><h3>Protected access</h3><p>A verification code is required after your password.</p></article><article class="promo-feature"><x-ui.icon name="key" size="22" /><h3>Recovery codes</h3><p>Keep backup codes available for emergencies.</p></article></div></div></aside>
</main>
@endsection
