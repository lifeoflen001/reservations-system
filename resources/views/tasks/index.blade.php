@extends('layouts.app')

@php($edit = $editTask)
@section('content')
<x-page-header title="Tasks" subtitle="Manage hotel operational tasks, assignments and follow-ups."><a class="ui-button ui-button--primary" href="{{ route('tasks.index', ['new' => 1]) }}"><x-ui.icon name="plus" size="16" /> Create task</a></x-page-header>
@if($errors->any())<x-feedback.alert type="danger">Please review the highlighted task fields and try again.</x-feedback.alert>@endif

<div class="metric-grid task-metrics">
    <article class="metric-card"><div><span>Total tasks</span><strong>{{ number_format($metrics['total']) }}</strong><small>Active operational work</small></div><i class="metric-card__icon metric-card__icon--info"><x-ui.icon name="check-square" size="18" /></i></article>
    <article class="metric-card"><div><span>Pending tasks</span><strong>{{ number_format($metrics['pending']) }}</strong><small>New, pending or in progress</small></div><i class="metric-card__icon metric-card__icon--warning"><x-ui.icon name="history" size="18" /></i></article>
    <article class="metric-card"><div><span>Completed tasks</span><strong>{{ number_format($metrics['completed']) }}</strong><small>Successfully closed</small></div><i class="metric-card__icon metric-card__icon--success"><x-ui.icon name="check" size="18" /></i></article>
    <article class="metric-card"><div><span>Overdue tasks</span><strong>{{ number_format($metrics['overdue']) }}</strong><small>Needs attention now</small></div><i class="metric-card__icon metric-card__icon--danger"><x-ui.icon name="alert" size="18" /></i></article>
</div>

<section class="ui-card task-list-card">
    <header class="table-card__header task-list-card__header"><div><h2>All tasks</h2><p>Live operational work across your hotel departments.</p></div></header>
    <form class="filter-toolbar task-filter-toolbar" method="GET" action="{{ route('tasks.index') }}">
        <x-form.input name="search" value="{{ request('search') }}" placeholder="Search tasks, rooms, guests or codes..." aria-label="Search tasks" />
        <div class="task-date-range"><x-form.input name="date_from" type="date" value="{{ request('date_from') }}" aria-label="Due date from" /><span>to</span><x-form.input name="date_to" type="date" value="{{ request('date_to') }}" aria-label="Due date to" /></div>
        <x-form.select name="status" aria-label="Filter tasks by status"><option value="all">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status','all') === $status->value)>{{ $status->label() }}</option>@endforeach</x-form.select>
        <button class="ui-button ui-button--info" type="submit"><x-ui.icon name="filter" size="16" /> Filter</button>
        <details class="task-advanced-filter"><summary><x-ui.icon name="settings" size="15" /> Filters</summary><div class="task-advanced-filter__menu"><x-form.select name="department_id" label="Department"><option value="all">All departments</option>@foreach($departments as $department)<option value="{{ $department->id }}" @selected((string)request('department_id','all') === (string)$department->id)>{{ $department->name }}</option>@endforeach</x-form.select><x-form.select name="priority" label="Priority"><option value="all">All priorities</option>@foreach($priorities as $priority)<option value="{{ $priority->value }}" @selected(request('priority','all') === $priority->value)>{{ $priority->label() }}</option>@endforeach</x-form.select><x-form.select name="assignee_id" label="Assignee"><option value="all">All assignees</option>@foreach($staff as $person)<option value="{{ $person->id }}" @selected((string)request('assignee_id','all') === (string)$person->id)>{{ $person->display_name }}</option>@endforeach</x-form.select><x-form.select name="room_id" label="Room"><option value="all">All rooms</option>@foreach($rooms as $room)<option value="{{ $room->id }}" @selected((string)request('room_id','all') === (string)$room->id)>Room {{ $room->room_number }}</option>@endforeach</x-form.select><label class="checkbox-field"><input type="checkbox" name="overdue" value="1" @checked(request()->boolean('overdue'))><span>Overdue only</span></label></div></details>
        <a class="ui-button ui-button--secondary" href="{{ route('tasks.index') }}">Reset</a>
    </form>
    <x-data.table caption="Hotel operations tasks" class="task-table"><thead><tr><th>Task #</th><th>Department</th><th>Task</th><th>Related to</th><th>Assigned to</th><th>Due date</th><th>Status</th><th>Priority</th><th class="table-actions">Actions</th></tr></thead><tbody>
        @forelse($tasks as $task)
            @php($overdue = $task->due_at && $task->due_at->isPast() && !in_array($task->status->value, ['completed','cancelled'], true))
            <tr class="{{ $overdue ? 'task-row--overdue' : '' }}"><td><a class="reservation-code" href="{{ route('tasks.show', $task) }}">{{ $task->task_number }}</a></td><td>{{ $task->department?->name ?? 'General' }}</td><td><a class="task-table__title" href="{{ route('tasks.show', $task) }}">{{ $task->title }}</a><small class="table-muted">{{ Str::limit($task->description ?: ($task->category ?: 'Operational follow-up'), 65) }}</small></td><td><div class="task-context">@if($task->room)<a href="{{ route('rooms.index', ['room' => $task->room_id]) }}">Room {{ $task->room->room_number }}</a>@endif @if($task->reservation)<a href="{{ route('reservations.show', $task->reservation) }}">{{ $task->reservation->code }}</a>@endif @if($task->client)<span>{{ $task->client->full_name }}</span>@endif @if(!$task->room && !$task->reservation && !$task->client)<span>General</span>@endif</div></td><td><div class="task-avatar-stack" aria-label="{{ $task->assignees->pluck('display_name')->join(', ') ?: 'Unassigned' }}">@forelse($task->assignees->take(3) as $person)<span class="avatar avatar--small" title="{{ $person->display_name }}">{{ $person->initials ?: 'U' }}</span>@empty<span class="table-muted">Unassigned</span>@endforelse @if($task->assignees->count() > 3)<span class="avatar avatar--small task-avatar-stack__more">+{{ $task->assignees->count() - 3 }}</span>@endif</div></td><td><span class="{{ $overdue ? 'task-due--overdue' : '' }}">{{ $task->due_at?->format('d M Y, H:i') ?? 'No due date' }}</span>@if($overdue)<small class="task-overdue-label">Overdue</small>@endif</td><td><x-ui.badge :variant="$task->status->badgeVariant()">{{ $task->status->label() }}</x-ui.badge></td><td><x-ui.badge :variant="$task->priority->badgeVariant()">{{ $task->priority->label() }}</x-ui.badge></td><td class="table-actions"><div class="row-actions"><a class="icon-button" href="{{ route('tasks.show', $task) }}" aria-label="View task" data-tooltip="View task"><x-ui.icon name="eye" size="17" /></a>@can('update',$task)<a class="icon-button" href="{{ route('tasks.edit', $task) }}" aria-label="Edit task" data-tooltip="Edit task"><x-ui.icon name="edit" size="17" /></a>@endcan @can('complete',$task) @if(!in_array($task->status->value,['completed','cancelled'],true))<form method="POST" action="{{ route('tasks.complete', $task) }}">@csrf<button class="icon-button icon-button--success" type="submit" aria-label="Complete task" data-tooltip="Complete task"><x-ui.icon name="check" size="17" /></button></form>@endif @endcan @can('archive',$task)<form method="POST" action="{{ route('tasks.archive', $task) }}" data-confirm="Archive this operational task? Its history will be preserved.">@csrf<button class="icon-button icon-button--danger" type="submit" aria-label="Archive task" data-tooltip="Archive task"><x-ui.icon name="trash" size="17" /></button></form>@endcan</div></td></tr>
        @empty<tr><td colspan="9"><div class="empty-state"><x-ui.icon name="check-square" size="28" /><strong>No operational tasks found.</strong><span>Create a task or clear the current filters.</span></div></td></tr>@endforelse
    </tbody></x-data.table>
    @if($tasks->hasPages())<div class="pagination-wrap">{{ $tasks->links() }}</div>@endif
</section>

@if($openNew || $edit)
<x-ui.modal id="task-form" :title="$edit ? 'Edit task' : 'Create task'" size="large" open="true"><form method="POST" action="{{ $edit ? route('tasks.update', $edit) : route('tasks.store') }}" data-draft-form data-draft-lifecycle="task" data-draft-key="task-{{ $edit ? 'edit-'.$edit->id : 'new' }}">@csrf @if($edit) @method('PUT') @endif
    <div class="task-form-grid"><x-form.input name="title" label="Task title" value="{{ old('title', $edit?->title) }}" placeholder="e.g. Prepare Room 104 for VIP arrival" required fieldClass="task-form-grid__full" /><x-form.select name="department_id" label="Department"><option value="">General / no department</option>@foreach($departments as $department)<option value="{{ $department->id }}" @selected((string)old('department_id',$edit?->department_id)===(string)$department->id)>{{ $department->name }}</option>@endforeach</x-form.select><x-form.select name="priority" label="Priority" required>@foreach($priorities as $priority)<option value="{{ $priority->value }}" @selected(old('priority',$edit?->priority?->value ?? 'normal') === $priority->value)>{{ $priority->label() }}</option>@endforeach</x-form.select><x-form.select name="category" label="Task category"><option value="">Select category</option>@foreach($categories as $category)<option value="{{ $category }}" @selected(old('category',$edit?->category) === $category)>{{ $category }}</option>@endforeach</x-form.select><x-form.input name="due_at" type="datetime-local" label="Due date / time" value="{{ old('due_at',$edit?->due_at?->format('Y-m-d\TH:i')) }}" /><x-form.select name="room_id" label="Related room"><option value="">No room</option>@foreach($rooms as $room)<option value="{{ $room->id }}" @selected((string)old('room_id',$edit?->room_id)===(string)$room->id)>Room {{ $room->room_number }} · {{ $room->roomType?->name }}</option>@endforeach</x-form.select><x-form.select name="reservation_id" label="Related reservation"><option value="">No reservation</option>@foreach($reservations as $reservation)<option value="{{ $reservation->id }}" @selected((string)old('reservation_id',$edit?->reservation_id)===(string)$reservation->id)>{{ $reservation->code }} · {{ $reservation->client?->full_name }}</option>@endforeach</x-form.select><x-form.select name="client_id" label="Related guest"><option value="">No guest</option>@foreach($clients as $client)<option value="{{ $client->id }}" @selected((string)old('client_id',$edit?->client_id)===(string)$client->id)>{{ $client->full_name }}</option>@endforeach</x-form.select><x-form.select name="status" label="Status" required>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(old('status',$edit?->status?->value ?? 'new') === $status->value)>{{ $status->label() }}</option>@endforeach</x-form.select><x-form.input name="estimated_minutes" type="number" label="Estimated minutes" value="{{ old('estimated_minutes',$edit?->estimated_minutes) }}" /><div class="form-field task-form-grid__full"><span class="form-label">Assign to staff</span><div class="task-assignee-picker">@foreach($staff as $person)<label class="task-assignee-option"><input type="checkbox" name="assignee_ids[]" value="{{ $person->id }}" @checked($edit?->assignees?->contains('id',$person->id))><span class="avatar avatar--small">{{ $person->initials ?: 'U' }}</span><span><strong>{{ $person->display_name }}</strong><small>{{ $person->department?->name ?? $person->role?->label ?? 'Staff' }}</small></span></label>@endforeach</div></div><x-form.textarea name="description" label="Description / instructions" rows="4" value="{{ old('description',$edit?->description) }}" fieldClass="task-form-grid__full" /><x-form.input name="tags_text" label="Tags" value="{{ old('tags_text',$edit?->tags?->pluck('name')->join(', ')) }}" placeholder="VIP, Arrival, Follow-up" help="Separate tags with commas." fieldClass="task-form-grid__full" /></div>
    <div class="modal-form-footer"><span class="task-draft-status" data-draft-status role="status" aria-live="polite"></span><a class="ui-button ui-button--secondary" href="{{ route('tasks.index') }}">Cancel</a><button class="ui-button ui-button--primary" type="submit"><x-ui.icon name="save" size="16" /> {{ $edit ? 'Save changes' : 'Create task' }}</button></div>
</form></x-ui.modal>
@endif
@endsection
