@extends('layouts.app')
@section('content')
<x-page-header title="Announcement statistics" subtitle="View reach and engagement for this announcement."><a class="ui-button ui-button--secondary" href="{{ route('announcements.show',$announcement) }}">Back</a></x-page-header>
<section class="announcement-statistics ui-card"><header><h2>{{ $announcement->title }}</h2><x-ui.badge :variant="$announcement->is_high_priority ? 'danger' : 'neutral'">{{ $announcement->statusLabel() }}</x-ui.badge></header><div class="announcement-stat-grid"><article><strong>{{ number_format($total) }}</strong><span>Total recipients</span></article><article><strong>{{ number_format($views) }}</strong><span>Views</span></article><article><strong>{{ $total ? number_format(($views / $total) * 100, 0) : 0 }}%</strong><span>View rate</span></article><article><strong>{{ number_format($emails) }}</strong><span>Email deliveries</span></article></div><div class="announcement-progress\"><div><span>Views</span><strong>{{ $views }} / {{ $total }}</strong></div><div class="announcement-progress__track\"><span style="width:{{ $total ? min(100, ($views / $total) * 100) : 0 }}%"></span></div></div></section>
@endsection
